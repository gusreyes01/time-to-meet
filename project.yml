name: TimeToMeet
options:
  deploymentTarget:
    macOS: "13.0"
  developmentLanguage: en
  createIntermediateGroups: true
settings:
  base:
    SWIFT_VERSION: "5.9"
    MARKETING_VERSION: "1.1"
    CURRENT_PROJECT_VERSION: "3"
    DEVELOPMENT_TEAM: ""
    CODE_SIGN_STYLE: Automatic
targets:
  MeetingCore:
    type: framework
    platform: macOS
    deploymentTarget: "13.0"
    sources:
      - path: Sources/MeetingCore
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.alluxi.timetomeet.meetingcore
        DEFINES_MODULE: YES
        SKIP_INSTALL: YES
  TimeToMeet:
    type: application
    platform: macOS
    deploymentTarget: "13.0"
    sources:
      - path: Sources/TimeToMeet
      - path: Resources/AppIcon.icns
        buildPhase: resources
    dependencies:
      - target: MeetingCore
    settings:
      base:
        PRODUCT_NAME: "Time to Meet"
        EXECUTABLE_NAME: TimeToMeet
        PRODUCT_BUNDLE_IDENTIFIER: com.alluxi.timetomeet
        INFOPLIST_FILE: Resources/Info.plist
        CODE_SIGN_ENTITLEMENTS: Resources/TimeToMeet.entitlements
        ENABLE_HARDENED_RUNTIME: YES
        ENABLE_APP_SANDBOX: YES
        GENERATE_INFOPLIST_FILE: NO
